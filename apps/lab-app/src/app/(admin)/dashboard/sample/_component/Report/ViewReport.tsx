// Sample Management's report view is the exact same component the Patient Dashboard
// renders -- kept as a re-export so both entry points can never drift apart.
export { default } from "@/app/(admin)/component/patientDashboard/Report/ReportView";
