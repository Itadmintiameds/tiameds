export interface Feature {
    name: string;
    description: string;
    points: string[];
    imageSrc: string;
    imageAlt: string;
    link: string;
    icons ?: string; // Optional property for icons
  }